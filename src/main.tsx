import React from "react";
import ReactDOM from "react-dom/client";
import RomanticLovePage from "../teAmo.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RomanticLovePage />
  </React.StrictMode>
);
